mixin exp
    table.table
        thead
            tr
                th(scope="col") Date
                th(scope="col") Poste
                th(scope="col") Lieu
        each val in json.experiences
            tbody
                tr
                    td #{val.dates}
                    td #{val.content}
                    td #{val.place}
    
